import { madeToOrderSteps } from "@/components/homepage/homepage.data";
export function MadeToOrderSection() { return <section id="made-to-order" className="section made-to-order" aria-labelledby="made-to-order-title"><div className="container"><div className="section-header section-header--centered"><h2 id="made-to-order-title">How made-to-order works</h2><p>Every custom item is created especially for you, so your order needs a little making time before it is dispatched.</p></div><ol className="made-to-order__steps">{madeToOrderSteps.map((step, index) => <li key={step.title} className="made-to-order-card" data-animate="process-step"><span>{index + 1}</span><h3>{step.title}</h3><p>{step.text}</p></li>)}</ol><div className="made-to-order__notice" role="note"><strong>Made-to-order processing:</strong> please allow 3–5 working days before dispatch. If you need an item sooner, contact us before ordering.</div><div className="section-actions section-actions--centered"><a className="text-link" href="/pages/delivery-returns">Read Delivery &amp; Returns</a></div></div></section>; }
