import { reassuranceItems } from "@/components/homepage/homepage.data";
export function ReassuranceStrip() { return <section id="reassurance" className="reassurance-strip" aria-labelledby="reassurance-title"><div className="container"><h2 id="reassurance-title" className="sr-only">Why shop with Boo&amp;Brew</h2><ul className="reassurance-strip__grid">{reassuranceItems.map((item) => <li key={item.title} className="reassurance-item" data-animate="card-reveal"><span aria-hidden="true" className="reassurance-item__icon">{item.icon}</span><h3>{item.title}</h3><p>{item.text}</p></li>)}</ul></div></section>; }
