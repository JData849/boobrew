import { giftSets } from "@/components/homepage/homepage.data";
export function GiftSetsSection() { return <section id="gift-sets" className="section section--oat gift-sets" aria-labelledby="gift-sets-title"><div className="container gift-sets__inner"><div className="gift-sets__content"><p className="eyebrow">Gift-ready favourites</p><h2 id="gift-sets-title">Personalised pieces, packed together beautifully.</h2><p>Choose from ready-made gift sets or create a custom combination with a tumbler, mug, key ring, gift wrap and personal message.</p><div className="section-actions"><a className="button button--primary" href="/collections/gift-sets">Shop Gift Sets</a><a className="button button--secondary" href="/pages/custom-gifts">Build a Custom Gift</a></div></div><div className="gift-sets__cards">{giftSets.map((set) => <article className="gift-set-card" key={set.href} data-animate="card-reveal"><div className="gift-set-card__media"><span className="badge">{set.badge}</span><img src={set.image} alt={set.alt} loading="lazy" /></div><div><h3>{set.title}</h3><p>{set.includes}</p><p className="gift-set-card__price">{set.price}</p><a href={set.href} className="button button--small button--primary">{set.cta}</a></div></article>)}</div></div></section>; }
